export const DIMENSIONS = {
    CANVAS__WIDHT: window.innerWidth,
    CANVAS__HEIGHT: window.innerHeight,
};

export const SHIP__WIDTH = 100;
export const SHIP__HEIGHT = 100;

export const PLATFORM__WIDTH = 90;
export const PLATFORM__HEIGHT = 25;
